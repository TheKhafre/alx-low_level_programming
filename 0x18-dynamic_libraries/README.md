# Dynamic library. 

The dynamic library is an active library that can bw easily used across codebase. The functiins in the library have all been earlier created, however for this task, dummy files were created each containing the approprate prototype as seen in the header file.
