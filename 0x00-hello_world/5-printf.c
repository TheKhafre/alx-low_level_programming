#include <stdio.h>

/**
 * main - the program outputs a give phrase
 * followed by newline.
 * Return: 0 on success.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
